flask>=3.0.0
feedparser>=6.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
yt-dlp>=2024.0.0
gunicorn>=21.0.0
# Assembles rendered carousel pages into the PDF LinkedIn document posts need.
Pillow>=10.0.0
pytest>=8.0.0
# tests/test_telegram_bot.py uses @pytest.mark.asyncio; without this plugin
# pytest reports "async def functions are not natively supported".
pytest-asyncio>=0.23.0
python-telegram-bot[job-queue]>=20.7
yfinance>=0.2.0

# Postgres / Supabase (used when DATABASE_URL is set)
psycopg2-binary>=2.9.9
supabase>=2.4.0
stripe>=10.0.0,<15.0.0
